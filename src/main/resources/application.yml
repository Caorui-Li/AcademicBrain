spring:
  datasource:
    url: jdbc:mysql://localhost:3306/academic_brain?useSSL=false&serverTimezone=UTC
    username: root
    password: 110120Safety
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true